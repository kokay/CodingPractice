#include <iostream>
#include <vector>

using namespace std;

class ShufflingMachine {
public:
  double stackDeck(vector<int> shuffle, int maxShuffles, vector<int> cardsReceived, int K) {
    vector<int> card;
    for (int i = 0; i < shuffle.size(); ++i) card.push_back(i);

    for (int i = 0; i < maxShuffles; ++i) {

    }
  }
};